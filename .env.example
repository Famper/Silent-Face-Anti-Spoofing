# This stack expects a shared external Docker network named "webproxy" and
# the SimpleNginxProxy (or equivalent reverse proxy) to handle TLS and route
# traffic to anti-spoof-api on that network. Nothing in this stack publishes
# a host port, so there are no per-app vars to set here.
#
# Bootstrap once on the host:
#   docker network create webproxy
